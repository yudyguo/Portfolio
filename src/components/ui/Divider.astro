---
interface Props {
  variant?: 'light' | 'dark';
}
const { variant = 'light' } = Astro.props;
---

<hr
  class:list={[
    'border-0 h-px my-16 md:my-24',
    variant === 'dark' ? 'bg-white/10' : 'bg-black/5',
  ]}
  data-gsap="draw-line"
/>
